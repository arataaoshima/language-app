<p id="notice"><%= notice %></p>

<div class="title-wrapper">
  <h1 class="text-center"><%= @video.title %></h1>
</div>
<div class="container blog-table-wrapper">
<p class="text-center">
  <iframe width="560" height="315" src="<%= @video.url %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
</p>

<p>
  <div class="btn btn-outline-secondary"><%= @video.category.category_name %></div>
  <div class="btn btn-outline-secondary"><%= @video.group.name %></div>
</p>

<%= link_to 'Quiz', "/quiz/1/1/1" %> |
<%= link_to 'Edit', edit_video_path(@video) %> |
<%= link_to 'Back', videos_path %>
</div>
